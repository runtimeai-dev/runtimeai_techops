# RuntimeAI Discovery Scanners

> **MOVED to core repository**

The client-side agent discovery scripts (`scanner_unix.sh`, `scanner_windows.ps1`, etc.) are no longer maintained inside individual customer delivery folders. 

Because these agents represent universal functionality (detecting Shadow AI on host endpoints), they have been centralized into the core control plane.

**New Location:**
You can find these client scripts in the `runtimeai-enterprise` repository:
`runtimeai-enterprise/discovery/client_agents/`
